using UnityEngine;

public class KeyRockController : MonoBehaviour
{
    public bool hasKey;
    [SerializeField] private ObjectProperties objectProperties;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    public void KeyRockInteract()
    {
        //if(itemUsed == screwdriver)
        //  timeController.PassTimePanel
        //  ObtainKey();
        //else
        //  timeController.PassTimePanel
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
